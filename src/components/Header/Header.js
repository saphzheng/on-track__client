import './Header.scss';

const Header = () => {
    return (
        <header className="header">
            <img className="header__logo" alt="onTrack"></img>
            <h1 className="header__title">onTrack</h1>
        </header>
    );
}

export default Header;